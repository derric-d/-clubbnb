<h1>Review Show</h1>
<ul>
  <li>
    <%= @review.description %>|
    <%= @review.stars %>|
    <%= link_to "❌", flat_review_path(@flat), method: :delete, data: { confirm: 'are you sure' } %>

  </li>
</ul>


<div>
    <%= link_to "back", flat_path(@flat) %>
</div>
