<h1>Review Show</h1>
<ul>
  <li>
    <%= @review.description %>|
    <%= @review.stars %>|
    <%= link_to "❌", root_path, method: :delete, data: { confirm: 'are you sure' } %>
    <%#= raise %>
    <%= link_to "edit", edit_flat_booking_review_path(@flat, @review.booking, @review) %>

  </li>
</ul>


<div>
    <%= link_to "back", root_path %>
</div>
