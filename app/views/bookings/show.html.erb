<h1>Booking Show</h1>
<ul>
  <li>
    <%= @booking.move_in_date %>|
    <%= @booking.move_out_date %>|
    <%= link_to "❌", root_path, method: :delete, data: { confirm: 'are you sure' } %>
  </li>
</ul>
<div>
  <%= link_to "See this flats reviews", flat_booking_reviews_path(@flat, @booking) %>
</div>

<div>
  <%= link_to "Leave a review", new_flat_booking_review_path(@flat, @booking) %>
</div>


<div>
    <%= link_to "back", flat_path(@flat) %>
</div>
