<div class="container">
  <h1>Your Booking</h1>
    <p>Move-in date: <%= @booking.move_in_date %></p>
    <p>Move-out date: <%= @booking.move_out_date %></p>

  <div>
    <%= link_to "Delete Booking  |  ", root_path, method: :delete, data: { confirm: 'are you sure' } %>
    <%= link_to "See Reviews  |  ", flat_booking_reviews_path(@flat, @booking) %>
    <%= link_to "Write a Review", new_flat_booking_review_path(@flat, @booking) %>
  </div>

  <div>
      <%= link_to "Back", flat_path(@flat) %>
  </div>
</div>
